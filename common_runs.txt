# star start (set directory and environment)
cd code/job-post-nlp
conda activate jobpostnlp

# common runs to do (so we don't have to keep writting them, could also write a .bat):


dvc exp run --queue -S "prepare/preprocessing=lgmodel,simple" -S "prepare/settings=large_star" -S "prepare/star=star" -S "train/corex=flex3_specific" -S "train.corex.n_topics=20"


dvc exp run --queue -S "prepare/settings=full_star" -S "prepare/star=star" -S "train/corex=sockin,anchors,worklife"

dvc exp run --queue -S "prepare/settings=full_star" -S "prepare/star=star" -S "train/corex=sockin"

dvc exp run --queue -S "prepare/settings=small" -S "prepare/star=star" -S "train/corex=worklife"

dvc exp run --queue -S "prepare/settings=full_star" -S "prepare/star=star" -S "train/corex=flex3,flex3_specific" -S "train.corex.n_topics=5,10,15,20,30,50,100"

dvc exp run --queue -S "prepare/settings=full_star" -S "prepare/star=star" -S "train/corex=plain" -S "train.corex.n_topics=50"


dvc exp run --queue -S "prepare/tdm=n3_binary_keepall" -S "prepare/settings=full_star" -S "prepare/star=star" -S "train/corex=plain" -S "train.corex.n_topics=3"




dvc exp run --queue -S "prepare/settings=full_star" -S "prepare/star=star" -S "train/corex=flex3_specific" -S "train.corex.n_topics=5"


dvc exp run --queue -S "prepare/settings=full_star" -S "prepare/star=star" -S "train/corex=flex3,flex3_specific,flex3_plus,foreslag,brancher,escudero_brancher,escudero" -S "train.corex.n_topics=30,40,50"

dvc exp run --queue -S "prepare/settings=full_star" -S "prepare/star=star" -S "train/corex=flex1" -S "train.corex.anchor_strength=2,4,6,9,12" -S "train.corex.n_topics=50"

dvc exp run --queue -S "prepare/settings=full_star" -S "prepare/star=star" -S "train/corex=flex1_2,flex2_2,flex3_2"


dvc queue start -j7

dvc queue logs 0845550 --follow

dvc queue stop --kill

dvc queue status

You can use the following commands in your SSH terminal to monitor memory and CPU usage:

- `top`
  Shows real-time CPU and memory usage for all processes.

- `htop`
  An enhanced version of `top` (if installed). Use arrow keys to navigate.

- `free -h`
  Displays total, used, and free memory.

- `vmstat 1`
  Shows system performance statistics, updated every second.

- `ps aux --sort=-%mem | head`
  Lists top processes by memory usage.

- `ps aux --sort=-%cpu | head`
  Lists top processes by CPU usage.

You can run these commands directly in your SSH terminal.


Press q to stop





# cleaning up git, if you have made changes in main:
make new branch with your changes:
git checkout -b new-branch-name
